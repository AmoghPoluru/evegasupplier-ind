# TODO — Vercel Blob for product & media images (`evegasupply/`)

Reference implementation: **`evega/`** — `src/lib/vercel-blob-storage.ts`, `src/app/api/media/route.ts`, `src/app/api/media/file/[...path]/route.ts`, optional `src/app/api/media/create-from-url/route.ts`, optional `scripts/migrate-media-to-blob.ts`.

**Goal:** File bytes in **Vercel Blob**; metadata and **`media` id** in **MongoDB** (Payload). **`ImageUpload`** keeps calling **`POST /api/media`** and expects **`{ doc: media }`** with **`doc.id`**.

**Context today:** Payload **`Media`** uses **`staticDir: 'media'`** and Sharp **`imageSizes`**; there is **no** custom `app/api/media` route in repo yet; URLs mix **`/media/…`**, **`/api/media/…`**, **`/api/media/file/…`**; **`media`** is linked from Products, Buyers, Vendors, RFQs, Inquiries, Messages, BdoChatMessages, ProductCatalogs.

_See also: `doc/TODO_TODAY.md`._

---

1. Product decisions (**aligned with `evega/`** — recorded 2026-05-15)
   - **Tech**: Policy locked for implementation; tweak only via explicit change
   - **Details**:
     - **Max size / MIME (`POST /api/media`)**: Same as **`evega`**: no strict server allowlist in v1; accept multipart **`file`**, log size, **warn in server logs** if **> 100 MB**; hard cap = platform / Next body limit (raise **`next.config`** if we later allow very large files like **`evega`** videos). **`ImageUpload`** in **`evegasupply`** may keep stricter **client** rules (**~5 MB**, **jpeg / png / webp** only) for catalog/RFQ/inquiry flows until we choose to relax to **`evega`**-style warnings.
     - **Derivatives**: Same as **`evega`**: **Blob-backed `media` docs = single public `url`**; do **not** depend on Payload **`imageSizes`** for new Blob uploads. Use **`next/image`** + **`remotePatterns`** for Blob. **Hybrid**: existing **local** rows can keep **`imageSizes`** / relative URLs until **task 14** migration or on-demand backfill.
     - **Upload path**: **Primary = `POST /api/media`** (browser → Next → Vercel Blob with **`BLOB_READ_WRITE_TOKEN`**, **`put`** + **`addRandomSuffix`**, same flow as **`evega`**). **`POST /api/media/create-from-url`** = **optional** later (browser-direct Blob), parity with **`evega`**, not required for v1.
     - **Production**: **`BLOB_READ_WRITE_TOKEN` required**; if Blob upload unavailable and **`NODE_ENV === 'production'`**, return **500** with explicit config message (**no** prod fallback to local disk), same as **`evega`**. **Development**: without token, **local `payload.create` + `file`** fallback to **`media/`**.
     - **Route limits**: Use **`runtime = 'nodejs'`**; set **`maxDuration`** (e.g. **300s** on applicable plan if large uploads matter); mirror **`evega`** **`next.config`** patterns when raising body limits beyond defaults.
   - **Status**: Decided

2. Vercel Blob and environment variables
   - **Tech**: Vercel dashboard + env docs
   - **Details**:
     - **Yes — use the same env var names as `evega/`**. Blob code relies on the official **`@vercel/blob`** token: **`BLOB_READ_WRITE_TOKEN`**. There is no separate “evega-only” name; copy the **pattern** from **`evega`**, not the secret value.
     - **Per deployment**: attach a Blob store to the **`evegasupply` Vercel project** (or the env where this app runs). **Production** and **Preview** each need **`BLOB_READ_WRITE_TOKEN`** set to that project’s token. **`evega`** and **`evegasupply`** should use **different token values** if they are different projects/stores (do not point both prod apps at one token unless you intentionally share one Blob store).
     - **Local**: add **`BLOB_READ_WRITE_TOKEN`** to **`.env.local`** when testing real Blob uploads; document placeholder in **`.env.example`** (no real secret). Process mirrors **`evega/docs/HOW_TO_GET_BLOB_TOKEN.md`** / **`GET_BLOB_TOKEN_QUICK.md`**.
     - **App URL**: keep **`NEXT_PUBLIC_APP_URL`** (and **`NEXTAUTH_URL`** if you use it) consistent with the deployed origin. Payload **`serverURL`** in **`src/payload.config.ts`** is **`NEXT_PUBLIC_APP_URL || NEXTAUTH_URL || http://localhost:3000`** (same pattern as **`evega`**).
   - **Status**: Complete — **`serverURL`** added in **`src/payload.config.ts`**; **`/.env.example`** + **`doc/PAYLOAD_SETUP.md`** document **`BLOB_READ_WRITE_TOKEN`** and app URL.

3. Add `@vercel/blob` dependency
   - **Tech**: `package.json` + lockfile
   - **Details**:
     - Add **`@vercel/blob`** (version **`^2.3.1`**, same as **`evega`**)
     - Install and commit lockfile
   - **Status**: Complete — **`@vercel/blob@^2.3.1`** in **`package.json`**; **`npm install`** updated **`package-lock.json`**.

4. Implement `src/lib/vercel-blob-storage.ts`
   - **Tech**: Shared Blob helpers (mirror **`evega`**)
   - **Details**:
     - **`uploadToBlob`**, **`deleteFromBlob`**, optional **`checkBlobExists`**
     - Use **`put`** with **`access: 'public'`**, **`addRandomSuffix: true`**, **`contentType`** when known, **`BLOB_READ_WRITE_TOKEN`**
     - Throw clear errors when token missing (callers handle HTTP mapping)
   - **Status**: Complete — **`src/lib/vercel-blob-storage.ts`** added (parity with **`evega`**, slightly stricter `unknown` error typing).

5. Implement `POST /api/media`
   - **Tech**: `src/app/api/media/route.ts`, Payload auth, multipart upload
   - **Details**:
     - **`export const runtime = 'nodejs'`**
     - **`getPayload`** + **`payload.auth({ headers })`** → **401** if no user
     - Parse **`multipart/form-data`** **`file`**; validate size/MIME per decisions (task 1)
     - With token: buffer file → **`uploadToBlob`** → build **`media`** fields: **`alt`**, **`filename`**, **`mimeType`**, **`filesize`**, **`url`** (Blob URL)
     - Persist via **`payload.db.create`** or **`payload.create`** — match **`evega`** + Payload 3 + **`Media`** hooks so types stay valid; run **`npm run generate:types`** after **`Media`** changes
     - Return **`NextResponse.json({ doc: media })`** for **`ImageUpload`**
     - **Production** without token: **500** + clear configuration message
     - **Development** without token: fallback **`payload.create`** with **`file`** to local **`media/`** ( **`evega`** pattern)
     - Log server errors; do not leak secrets in JSON
   - **Status**: Complete — **`src/app/api/media/route.ts`** implements **`POST`** + **`DELETE`** (Blob + local dev); server requires **`image/*`** to match **`Media`** **`mimeTypes`**.

6. Implement `DELETE /api/media`
   - **Tech**: Same `route.ts` or split `DELETE` handler
   - **Details**:
     - Authenticate like task 5
     - Parse Payload-style query: **`where[and][0][id][in][n]`**, **`id`**, JSON **`where`** (mirror **`evega`**)
     - Per id: **`findByID`** → if **`url`** contains **`blob.vercel-storage.com`**, **`deleteFromBlob`** (log if blob delete fails, then continue) → **`payload.delete`**
     - Response: **`deleted`** ids, optional **`errors`**; status if all fail
   - **Status**: Complete — implemented in **`src/app/api/media/route.ts`** alongside **`POST`**.

7. (Optional) Implement `GET /api/media/file/[...path]`
   - **Tech**: `src/app/api/media/file/[...path]/route.ts`
   - **Details**:
     - Resolve **`media`** by filename (or id — match **`evega`**); reject **`..`** / traversal
     - If **`doc.url`** is absolute **http(s)** → **redirect** to Blob
     - Else stream from local **`media/`** if file exists; set **Content-Type** + cache headers
     - Document: local path is **dev / legacy** on Vercel
   - **Status**: Pending

8. (Optional) Implement `POST /api/media/create-from-url`
   - **Tech**: `src/app/api/media/create-from-url/route.ts`
   - **Details**:
     - Only if adopting **browser → Blob** direct upload (task 1)
     - Auth required; JSON **`{ url, filename, mimeType?, filesize?, alt? }`**
     - Validate Blob host; **`payload.create`** **`media`**; return **`{ doc }`**
   - **Status**: Pending

9. Route duration and body size limits
   - **Tech**: Next.js route segment config + Vercel plan
   - **Details**:
     - Set **`maxDuration`** for worst-case upload time
     - Confirm Vercel / Next body limits vs max file size; align **`ImageUpload`** **`maxSize`** if needed
   - **Status**: Pending

10. Reconcile Payload `Media` collection for Blob rows
   - **Tech**: `src/collections/Media.ts` + `generate:types`
   - **Details**:
     - Align **`staticDir`**, **`imageSizes`**, **`mimeTypes`**, **`alt`** (required vs optional) with DB-only / remote-**`url`** docs
     - Tighten **`access`** (**`create` / `update` / `delete`**) for production B2B roles
     - Run **`npm run generate:types`** after schema edits
   - **Status**: Pending

11. Canonical media URL helper and `next/image`
   - **Tech**: e.g. `src/lib/media-url.ts`, `next.config.ts`
   - **Details**:
     - **`getMediaUrl`** (populated **`Media`** or id): return absolute **`url`** if present, else single stable proxy path (**`/api/media/file/…`** or agreed pattern)
     - **`next.config.ts`**: **`images.remotePatterns`** for **`*.blob.vercel-storage.com`**
     - Replace ad-hoc URLs with helper in:
       - `src/components/ui/image-upload.tsx` (preview + **`onError`** fallback)
       - `src/app/(app)/vendor/products/[id]/page.tsx`
       - `src/app/(app)/buyer/rfqs/new/components/RFQForm.tsx`
       - `src/app/(app)/buyer/inquiries/new/components/InquiryForm.tsx` (if preview by id)
       - `src/app/(app)/buyer/inquiries/[id]/InquiryDetailClient.tsx`
     - Confirm **`ProductCard.tsx`** / **`ProductCardHorizontal.tsx`** work with Blob **`url`** (mostly **`next/image`** domain)
     - Grep for leftover **`/media/`** or **`/api/media/`** string builds; remove drift
   - **Status**: Pending

12. Server-side delete: remove Blob before deleting `media` docs
   - **Tech**: tRPC routers + shared delete helper
   - **Details**:
     - `src/trpc/routers/admin.ts`: when deleting **`media`**, call **`deleteFromBlob`** if **`url`** is Vercel Blob, then **`payload.delete`**
     - `src/trpc/routers/vendors.ts` (or related): same when removing product images / media per product rules
     - Extract one **`deleteMediaWithBlob`** helper shared with task 6 to avoid drift
     - Document orphan Blob policy (acceptable vs cleanup job)
   - **Status**: Pending

13. Audit tRPC / queries for populated `media` (`url` on read path)
   - **Tech**: Product, cart, profile queries
   - **Details**:
     - Ensure **`images`** (and logo fields) are populated deeply enough that **`url`** is available for cards and detail pages, or extend helper to resolve by id
     - Verify buyer/vendor logo UIs after migration
   - **Status**: Pending

14. (Optional) Migrate existing local `media/` files to Blob
   - **Tech**: Script + npm command (from **`evega`** `migrate-media-to-blob.ts`)
   - **Details**:
     - For each **`media`** row without Blob **`url`**, read local file → **`uploadToBlob`** → update **`url`** / **`filename`**
     - Add script e.g. **`db:migrate:media:blob`**
     - Document: backup Mongo + **`media/`**; idempotent / safe rerun
   - **Status**: Pending

15. Documentation and deployment handoff
   - **Tech**: README or internal ops doc
   - **Details**:
     - Blob token, prod vs local upload behavior, env var list
     - Point to **`evega`** for future parity (videos, longer **`maxDuration`**, etc.)
   - **Status**: Pending

16. QA — end-to-end verification
   - **Tech**: Manual + staging
   - **Details**:
     - Vendor **`ProductForm`**: multi-image upload, save, reload
     - Admin **`AdminProductForm`**: same
     - Buyer RFQ / inquiry **`ImageUpload`** and detail links
     - Marketplace **`ProductCard`** / horizontal + **`/products/[productId]`** gallery (**`next/image`**)
     - Remove/replace images: DB and Blob match chosen orphan policy
     - Payload Admin **media** delete removes Blob object (if using admin UI)
     - Prod-like: no token → clear upload failure; local dev: no token → local **`payload.create`** still works
   - **Status**: Pending
